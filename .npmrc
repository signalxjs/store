# Allow internal @sigx packages to be hoisted so TypeScript can resolve types
public-hoist-pattern[]=@sigx/*

# Hoist @types so tsgo can find ambient packages like @types/node
public-hoist-pattern[]=@types/*

# Resolve workspace packages for peer dependencies
link-workspace-packages=true
